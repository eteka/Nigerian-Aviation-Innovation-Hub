# Production Environment Configuration
NODE_ENV=production

# PostgreSQL Configuration (REQUIRED for production)
# Option 1: Use DATABASE_URL (recommended for Heroku, Railway, Render, etc.)
DATABASE_URL=postgresql://username:password@hostname:5432/aviation_hub

# Option 2: Use individual connection parameters
# DB_HOST=your-postgres-host.com
# DB_PORT=5432
# DB_NAME=aviation_hub
# DB_USER=your_db_user
# DB_PASSWORD=your_secure_password
# DB_SSL=true

# JWT Secret (REQUIRED - generate a secure random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Initial Admin Setup (for first-time setup)
INITIAL_ADMIN_EMAIL=admin@aviation.gov.ng
INITIAL_ADMIN_PASSWORD=SecurePassword123!
INITIAL_ADMIN_NAME=System Administrator

# Server Configuration
PORT=3000

# CORS Configuration (set to your frontend domain)
CORS_ORIGIN=https://yourdomain.com
