# Environment Configuration
NODE_ENV=development

# Development/Test uses SQLite (no additional config needed)
# Production uses PostgreSQL (configure below)

# PostgreSQL Configuration (Production only)
# Option 1: Use DATABASE_URL (recommended for cloud deployments)
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Option 2: Use individual connection parameters
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=aviation_hub
# DB_USER=postgres
# DB_PASSWORD=your_password_here
# DB_SSL=false

# JWT Secret (REQUIRED - generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Initial Admin Setup (for production seed script)
INITIAL_ADMIN_EMAIL=admin@aviation.gov.ng
INITIAL_ADMIN_PASSWORD=ChangeMe123!
INITIAL_ADMIN_NAME=System Administrator

# Server Configuration
PORT=3000

# CORS Configuration (optional)
# CORS_ORIGIN=https://yourdomain.com
